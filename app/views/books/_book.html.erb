
   
      <tr>
        <td><%= @book.name %></td>
        <td><%= @book.phone %></td>
        
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?', remote: true }, class: 'delete' %></td>
      </tr>
  
